apiversion : eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: expense-Poject
  region: ap-south-1

managedNodeGroups:
  - name: exp-worker
    instanceType: m5.large
    desiredCapacity: 3
    spot: true